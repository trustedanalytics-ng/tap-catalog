---
  kind: "Service"
  apiVersion: "v1"
  metadata:
    name: "catalog"
    labels:
      id: "catalog"
  spec:
    type: "NodePort"
    selector:
      id: "catalog"
    ports:
      -
        port: 80
        protocol: "TCP"
        name: "rest"
